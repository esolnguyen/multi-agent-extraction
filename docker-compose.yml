services:
  extraction:
    build: .
    env_file: docker/.env
    environment:
      - FLOW_CONFIG_PATH=/app/flow_config.json
      - INPUT_PDF_PATH=/app/data/input.pdf
      - OUTPUT_DIR=/app/data/output
    volumes:
      - ./data:/app/data
